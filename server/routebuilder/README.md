# Running wmt routebuilder

This is the routebuilder from waymarked trails.

````shell
python3 -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/pip install shapely psycopg2-binary python-dotenv pytest
```
